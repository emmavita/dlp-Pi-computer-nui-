# assets/

Static assets for the projected UI (e.g. calibration patterns, fonts). None are
bundled in this prototype; the calibration chessboard is generated/handled by
`tools/calibration/`. Add binary assets here as the UI grows.
