# Examples

- `pinch_dataset.example.json` — labelled samples for
  `tools/calibration/calibrate_pinch.py`.
- `homography_correspondences.example.json` — camera↔surface point pairs for
  `tools/calibration/compute_homography.py`.

These are illustrative synthetic inputs to exercise the tools; real calibration
data must be captured on the target.
